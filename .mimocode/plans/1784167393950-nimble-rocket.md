# Guia de Correcao — CRM Vendedores + Permissoes + Encoding

## Arquivo 1: `public/funcionario.html`

### Mudanca A: Adicionar data-group nos grupos da sidebar

Encontre as 4 divs `.sidebar-grupo` e adicione o atributo `data-group`:

**Linha ~37** (Calculadoras):
```html
<!-- DE: -->
<div class="sidebar-grupo">

<!-- PARA: -->
<div class="sidebar-grupo" data-group="calculadoras">
```

**Linha ~57** (Vendedores/Comercial):
```html
<!-- DE: -->
<div class="sidebar-grupo">

<!-- PARA: -->
<div class="sidebar-grupo" data-group="comercial">
```

**Linha ~74** (Producao):
```html
<!-- DE: -->
<div class="sidebar-grupo">

<!-- PARA: -->
<div class="sidebar-grupo" data-group="producao">
```

**Linha ~85** (Logistica):
```html
<!-- DE: -->
<div class="sidebar-grupo">

<!-- PARA: -->
<div class="sidebar-grupo" data-group="logistica">
```

---

## Arquivo 2: `public/js/funcionario.js`

### Mudanca B: Fix vendedores no crmRenderizar() (linha ~1188-1207)

Encontre o bloco try/catch que carrega vendedores no `crmRenderizar()` e troque por:

```javascript
  // Carregar vendedores da API para o filtro
  let vendedoresNomes = [];
  try {
    const resp = await fetch('/api/vendedores', {
      headers: { 'Authorization': 'Bearer ' + token }
    });
    if (!resp.ok) throw new Error('API ' + resp.status);
    const vendedoresAPI = await resp.json();
    vendedoresNomes = vendedoresAPI.map(v => v.login);
  } catch (e) {
    console.warn('Erro ao carregar vendedores:', e);
    vendedoresNomes = [...new Set(clientes.map(c => c.vendedor).filter(Boolean))];
  }
  // Adicionar vendedores de clientes que possam nao estar na API
  clientes.forEach(c => {
    if (c.vendedor && !vendedoresNomes.includes(c.vendedor)) {
      vendedoresNomes.push(c.vendedor);
    }
  });
  // Fallback final: se ainda vazio, usar apenas clientes
  if (vendedoresNomes.length === 0) {
    vendedoresNomes = [...new Set(clientes.map(c => c.vendedor).filter(Boolean))];
  }
```

A diferenca principal e a linha `if (!resp.ok) throw new Error(...)` que faz o catch executar em respostas 401/403/etc.

### Mudanca C: Fix vendedores no carregarVendedoresSelect() (linha ~1341-1366)

Encontre a funcao `carregarVendedoresSelect` e troque o bloco catch por:

```javascript
  } catch (err) {
    console.warn('Erro ao carregar vendedores:', err);
    // Fallback: vendedores dos clientes cadastrados
    const clientesSalvos = await crmCarregarClientes().catch(() => []);
    const fallback = [...new Set(clientesSalvos.map(c => c.vendedor).filter(Boolean))];
    select.innerHTML = '<option value="">Selecione o vendedor</option>';
    fallback.forEach(v => {
      select.innerHTML += `<option value="${v}" ${v === vendedorAtual ? 'selected' : ''}>${v}</option>`;
    });
  }
```

### Mudanca D: Filtrar sidebar por cargo (linha ~1668, apos os blocos de permissao existentes)

Adicione ESTE BLOCO apos a linha que esconde o toolbar de fotos (linha ~1678):

```javascript
  // Filtrar sidebar por cargo
  const permissoesGrupos = {
    admin:      ['calculadoras', 'comercial', 'producao', 'logistica'],
    engenheiro: ['calculadoras', 'comercial', 'producao', 'logistica'],
    vendedor:   ['calculadoras', 'comercial'],
    logistica:  ['calculadoras', 'logistica']
  };
  const gruposPermitidos = permissoesGrupos[user?.cargo] || permissoesGrupos.vendedor;
  document.querySelectorAll('.sidebar-grupo[data-group]').forEach(grupo => {
    if (!gruposPermitidos.includes(grupo.dataset.group)) {
      grupo.style.display = 'none';
    }
  });
```

### Mudanca E: Guard na funcao trocarTela() (linha ~68)

Encontre a funcao `trocarTela` e adicione verificacao de permissao NO INICIO:

```javascript
function trocarTela(tela) {
  // Guard: verificar permissao por cargo
  const telasPermitidas = {
    admin:      ['producao', 'conversor', 'mureta', 'malha', 'fotos', 'crm', 'cancelamentos'],
    engenheiro: ['producao', 'conversor', 'mureta', 'malha', 'fotos', 'crm', 'cancelamentos'],
    vendedor:   ['producao', 'conversor', 'mureta', 'malha', 'crm', 'cancelamentos', 'fotos'],
    logistica:  ['producao', 'conversor', 'mureta', 'malha', 'fotos']
  };
  const permitidas = telasPermitidas[user?.cargo] || telasPermitidas.vendedor;
  if (!permitidas.includes(tela)) tela = 'producao';

  document.querySelectorAll('.tela').forEach(t => t.classList.remove('ativa'));
  // ... resto da funcao continua igual
```

### Mudanca F: Fix mojibake (caracteres corrompidos)

Encontre e substitua TODAS as ocorrencias destes padroes no arquivo:

| Buscar | Substituir por |
|--------|---------------|
| `â€"` | `—` (traco longo) |
| `ðŸ'¥` | `&#128100;` (emoji pessoa) |
| `ðŸ"·` | `&#128247;` (emoji camera) |
| `ðŸ"` | `&#128193;` (emoji pasta) |
| `ðŸ–¼ï¸` | `&#128444;` (emoji imagem) |
| `âš ï¸` | `&#9888;` (emoji alerta) |
| `Ã§` | `ç` |
| `Ã£` | `ã` |
| `Ã¡` | `á` |
| `Ã³` | `ó` |
| `Ãµ` | `õ` |
| `Ãª` | `ê` |
| `Ã´` | `ô` |
| `Ã ` | `à` |
| `Ã•` | `Õ` |
| `Ã‰` | `É` |
| `Ã` | `Á` |

Use "Replace All" no editor para cada um. Se preferir, pode ignorar os comentarios (as linhas que comecam com `//`), pois so afetam o codigo fonte, nao o usuario.

A forma mais facil: no VS Code, abra `funcionario.js`, pressione `Ctrl+H`, ative "Use Regular Expression" (icone `.*`), e faca replace de cada padrao.

---

## Verificacao apos aplicar

1. Abra o site e logue como **Miqueias** (vendedor) → deve ver: Calculadoras + Vendedores/Comercial
2. Logue como **Kelvin** (logistica) → deve ver: Calculadoras + Logistica
3. Logue como **Admin** → deve ver todos os grupos
4. No CRM, o dropdown "Todos os vendedores" deve mostrar: Miqueias, Andre, Daniela
5. Tela CRM vazio deve mostrar icone correto (sem caracteres estranhos)
6. Clique em "Criar Cliente" → modal abre, select de vendedores popula
